# ─── DynamoDB Local ─────────────────────────────────────────────────────
DYNAMODB_TABLE=packpixie-test
LOCAL_DYNAMODB_URL=http://localhost:8000
AWS_REGION=us-east-1

# ─── API server ──────────────────────────────────────────────────────────
PORT=3001

# ─── Cognito (get from AWS Console → Cognito → User Pools) ──────────────
COGNITO_USER_POOL_ID="us-east-1_ao1USvs5A"
COGNITO_CLIENT_ID="78vhsc42eskpml05i9mqcbv89l"

# ─── Test user credentials (create via AWS CLI — see README.md) ─────────
TEST_USER_EMAIL=pekka.pakkaaja@pekkis.com
TEST_USER_PASSWORD=pekka04840039

# ─── Vite client env vars ────────────────────────────────────────────────
# VITE_APP_VERSION is required by apps/client/src/config.ts — must not be blank
VITE_APP_VERSION=test
VITE_API_URL=http://localhost:3001
VITE_COGNITO_USER_POOL_ID="us-east-1_ao1USvs5A"
VITE_COGNITO_USER_POOL_CLIENT_ID="78vhsc42eskpml05i9mqcbv89l"
