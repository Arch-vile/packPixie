---
phase: 07-item-write-api-server-authoritative-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/model/src/item.ts
  - apps/api/src/lib/tripDetail.ts
  - apps/api/src/lib/tripDetail.test.ts
  - apps/api/src/routes/api.ts
  - apps/api/package.json
autonomous: true
requirements: [ITEM-04, ITEM-05, ITEM-06]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POST /trips/:tripId/items creates a new item with a fresh crypto.randomUUID() itemId, writes Name/Qty/Consumable always and Weight/PackedBy/Status/Category only when supplied, and returns 201 with the created Item (D-01/D-02)"
    - "A raw POST or PATCH request cannot result in Status: 'packed' unless the resulting PackedBy is a non-empty value — the server rejects with 400 { error: 'packed requires PackedBy to be set' } regardless of client (ITEM-04, roadmap SC#1)"
    - "Setting status: 'packed' while packedBy is omitted, null, or an empty/whitespace string is rejected with 400 on both create and patch (ITEM-04/empty)"
    - "PATCH clearing packedBy (packedBy: null) atomically removes both PackedBy and Status from the item in a single UpdateItem call, so the two attributes can never diverge (ITEM-05, roadmap SC#2)"
    - "A single PATCH that both clears packedBy (packedBy: null) and sets status: 'packed' in the same request is rejected with the same 400 packed-requires-PackedBy error — D-06's resulting-field computation runs before the invariant check, so the two rules compose without any special-case branch (ITEM-05/unclassified, D-06)"
    - "Quantity and Weight are always written to DynamoDB as native numbers, never strings; a non-numeric quantity or weight value is rejected with 400 (ITEM-06, roadmap SC#3)"
    - "An item created or patched without a weight has no Weight attribute in storage (mapItemRecord reads it back as undefined); an item explicitly given weight: 0 stores Weight: 0 and reads back as 0 — the two are distinguishable (ITEM-06, roadmap SC#3)"
    - "PackedBy set to an email that is not one of the trip's participants is rejected with 400 { error: 'PackedBy must be a trip participant' } on both create and patch (D-04)"
    - "POST and PATCH reject any request-body field outside name/quantity/weight/packedBy/status/category/consumable with 400 { error: 'Unknown field: <name>' } (D-05)"
    - "Every POST/PATCH/DELETE on /trips/:tripId/items requires the caller's lowercased JWT email to match a USER# record for the trip; a non-member and a caller of a non-existent tripId both receive an identical 404 { error: 'Trip not found' } (roadmap SC#4, TRIP-02/03 carried into the write path)"
    - "PATCH or DELETE of an itemId with no matching ITEM# record in an existing trip returns 404 { error: 'Item not found' } — no phantom-row upsert occurs (roadmap SC#4)"
    - "DELETE on an item whose Status is 'packed' is rejected with 400 { error: 'Cannot delete a packed item — unpack it first' }; DELETE on any other existing item succeeds and returns 204 with no body (D-08/D-09)"
    - statement: "Re-sending an identical PATCH body twice in a row produces the same resulting item state both times — each PATCH computes an absolute resulting field set from the current record rather than applying a delta, so it is idempotent by construction (ITEM-06/idempotency, D-07)"
      verification: backstop
    - statement: "Two participants concurrently writing the same item is last-write-wins with no version field, no optimistic-concurrency check, and no 409 response — this is a deliberate accepted simplicity tradeoff for this milestone, not a defect (ITEM-06/concurrency, D-07)"
      verification: backstop
  prohibitions:
    - "MUST NOT auto-assign or silently default PackedBy (e.g. to the caller's own email) when a client attempts to set status: 'packed' without supplying PackedBy — the server rejects with 400 and never silently attributes the packing action to someone the client did not name"
    - "MUST NOT allow any write path to persist Status: 'packed' while PackedBy is absent or being cleared in the same update — every mutation must route through the shared computeItemPatch/buildCreateItemAttributes invariant helpers rather than issuing a raw UpdateItem/PutItem that bypasses them"
    - "MUST NOT truncate, round, or otherwise reduce the precision of a fractional Quantity or Weight value when persisting it as a DynamoDB number"
  artifacts:
    - packages/model/src/item.ts
    - apps/api/src/lib/tripDetail.ts
    - apps/api/src/lib/tripDetail.test.ts
    - apps/api/src/routes/api.ts
  key_links:
    - "request.user.email (authPlugin) -> callerEmail = request.user.email.trim().toLowerCase() -> USER#<email> membership predicate, reused identically across POST/PATCH/DELETE (same pattern as Phase 6 GET)"
    - "buildCreateItemAttributes / computeItemPatch (apps/api/src/lib/tripDetail.ts) -> mapItemRecord (apps/api/src/lib/tripDetail.ts, Phase 6) — write-path output must be read back correctly by the existing storage->DTO mapper (D-04 cross-phase contract)"
    - "computeItemPatch resulting-field computation -> buildUpdateExpression -> UpdateCommand ExpressionAttributeNames aliases — Name and Status are DynamoDB reserved words and must never appear unaliased in an UpdateExpression"
    - "PATCH UpdateCommand ConditionExpression: attribute_exists(PK) -> ConditionalCheckFailedException -> 404 { error: 'Item not found' } — prevents UpdateItem's default upsert behavior from creating a phantom row for a concurrently-deleted item (roadmap SC#4)"
---

<objective>
Add three protected write endpoints for trip items to `apps/api` — `POST /trips/:tripId/items` (create), `PATCH /trips/:tripId/items/:itemId` (partial update, handles all edit cases including status/PackedBy changes), `DELETE /trips/:tripId/items/:itemId` (hard delete) — with every packing invariant (ITEM-04, ITEM-05, ITEM-06) enforced server-side through a shared, independently-tested validation core in `apps/api/src/lib/tripDetail.ts`, so the rules hold even against a directly-crafted request body.

Purpose: This is the write half of the milestone's security-first API surface — the literal point of the phase is that no client (present or future) can bypass the packing invariants by calling the API directly.
Output: `packages/model/src/item.ts` gains `CreateItemRequest`/`PatchItemRequest`; `apps/api/src/lib/tripDetail.ts` gains the invariant/validation core plus a `tripDetail.test.ts` unit-test file exercising it via Node's built-in test runner (`node:test`, run through the already-present `tsx` devDependency — no new package installed); `apps/api/src/routes/api.ts` gains the three routes.
</objective>

<execution_context>
@/Users/mikko.ravimo/git/personal/packPixie/.claude/gsd-core/workflows/execute-plan.md
@/Users/mikko.ravimo/git/personal/packPixie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-item-write-api-server-authoritative-rules/07-CONTEXT.md
@.planning/phases/07-item-write-api-server-authoritative-rules/07-PATTERNS.md
@.planning/phases/06-trip-detail-read-api-participation-guard/06-CONTEXT.md
@dynamoDB-architecture.md
</context>

<artifacts_this_phase_produces>
Symbols and touchpoints this plan creates (surface for Phase 8's client consumers):

- **Routes:** `POST /trips/:tripId/items`, `PATCH /trips/:tripId/items/:itemId`, `DELETE /trips/:tripId/items/:itemId` — all registered in the `protected_` block of `apps/api/src/routes/api.ts`, typed `Promise<Item>` (POST/PATCH) and `Promise<void>` (DELETE).
- **Model types:** `packages/model/src/item.ts` gains `CreateItemRequest` and `PatchItemRequest` interfaces.
- **Module (`apps/api/src/lib/tripDetail.ts`) new exports:** `ItemWriteResult<T>`, `ITEM_WRITABLE_FIELDS`, `findUnknownFields`, `isValidItemStatus`, `validateStatusRequiresPackedBy`, `validatePackedByParticipant`, `validateNumericField`, `isTripMember`, `extractParticipantEmails`, `findItemRecord`, `buildCreateItemAttributes`, `computeItemPatch`, `buildUpdateExpression`, `assertItemDeletable` — alongside the existing `mapItemRecord` (Phase 6).
- **New test file:** `apps/api/src/lib/tripDetail.test.ts` — the first test file in the repo, run via `pnpm --filter api test`.
- **Modified:** `apps/api/package.json` `"test"` script now runs `tsx --test src/lib/*.test.ts` instead of exiting 1.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Shared item-write validation core + POST /trips/:tripId/items (end-to-end)</name>
  <files>packages/model/src/item.ts, apps/api/src/lib/tripDetail.ts, apps/api/src/lib/tripDetail.test.ts, apps/api/src/routes/api.ts, apps/api/package.json</files>
  <read_first>
    - apps/api/src/lib/tripDetail.ts (the file being extended — existing `mapItemRecord`, its header comment documenting the D-04 storage-attribute contract you must match)
    - packages/model/src/item.ts (the file being extended — `Item`/`ItemStatus`/`TripDetailResponse`, field names verbatim)
    - apps/api/src/routes/api.ts (the file being modified — `POST /trips` early-return + id/timestamp convention at lines 101-186, `GET /trips/:tripId` participation-guard query at lines 240-291, existing `@packpixie/model` barrel import at lines 8-14, `protected_` register block at lines 97-292)
    - apps/api/src/plugins/auth.ts (`request.user.email` origin)
    - apps/api/src/lib/dynamodb.ts (shared `DynamoDBDocumentClient`; `wrapNumbers: false`)
    - apps/api/package.json (current `"test"` script, `tsx` devDependency version)
    - dynamoDB-architecture.md (§2 entity layout, §3 item attribute types, §5 UpdateItem shape)
    - .planning/phases/07-item-write-api-server-authoritative-rules/07-PATTERNS.md (analog line numbers, reserved-word and DynamoDB client caveats)
  </read_first>
  <behavior>
    Write `apps/api/src/lib/tripDetail.test.ts` FIRST, using `import { describe, test } from 'node:test'; import assert from 'node:assert/strict';`, importing the not-yet-written functions from `./tripDetail.js`, and run it (it will fail to import/compile) before implementing. Cases to cover:
    - `findUnknownFields({ name: 'x', bogus: 1 })` returns `['bogus']`; `findUnknownFields({ name: 'x' })` returns `[]`.
    - `isValidItemStatus('packed')` is `true`; `isValidItemStatus('lost')` is `false`.
    - `validateStatusRequiresPackedBy('packed', undefined)` returns a non-null error string; `validateStatusRequiresPackedBy('packed', '  ')` (whitespace-only) also errors; `validateStatusRequiresPackedBy('packed', 'a@b.com')` returns `null`; `validateStatusRequiresPackedBy('to-buy', undefined)` returns `null`.
    - `validatePackedByParticipant('A@B.com', ['a@b.com'])` returns `null` (case-insensitive match); `validatePackedByParticipant('x@y.com', ['a@b.com'])` returns a non-null error.
    - `validateNumericField(5, 'quantity')` returns `{ ok: true, value: 5 }`; `validateNumericField('5', 'quantity')` and `validateNumericField(-1, 'quantity')` and `validateNumericField(NaN, 'quantity')` all return `{ ok: false, error: ... }`.
    - `isTripMember` / `extractParticipantEmails` / `findItemRecord` against a small fixture array of `{ SK, Email }`-shaped records.
    - `buildCreateItemAttributes`: omitting `weight` yields no `Weight` key in the returned attributes; passing `weight: 0` yields `Weight: 0` present (0 distinguishable from absent, ITEM-06); omitting `quantity` defaults `Qty` to `1`; omitting `consumable` defaults `Consumable` to `false`; `status: 'packed'` with no `packedBy` returns `{ ok: false, error: ... }`; an unknown field returns `{ ok: false, error: ... }`; a `packedBy` not present in the given participant list returns `{ ok: false, error: ... }`.
  </behavior>
  <action>
    In `packages/model/src/item.ts`, add `export interface CreateItemRequest { name: string; quantity?: number; weight?: number; packedBy?: string; status?: ItemStatus; category?: string; consumable?: boolean; }`.

    In `apps/api/src/lib/tripDetail.ts`, add (alongside the existing `mapItemRecord`): a shared result type `export type ItemWriteResult<T> = { ok: true; value: T } | { ok: false; error: string };`; `export const ITEM_WRITABLE_FIELDS = ['name', 'quantity', 'weight', 'packedBy', 'status', 'category', 'consumable'] as const;`; `export function findUnknownFields(body: object): string[]` (keys of `body` not in `ITEM_WRITABLE_FIELDS`); `export function isValidItemStatus(value: unknown): value is ItemStatus` (`value === 'to-buy' || value === 'found' || value === 'packed'`); `export function validateStatusRequiresPackedBy(status: ItemStatus | undefined, packedBy: string | undefined): string | null` (error `'packed requires PackedBy to be set'` when `status === 'packed'` and `!packedBy?.trim()`); `export function validatePackedByParticipant(packedBy: string, participantEmails: string[]): string | null` (error `'PackedBy must be a trip participant'` unless `participantEmails.map(e => e.trim().toLowerCase())` includes `packedBy.trim().toLowerCase()`); `export function validateNumericField(value: unknown, fieldLabel: string): ItemWriteResult<number>` (error `` `${fieldLabel} must be a non-negative number` `` unless `typeof value === 'number' && Number.isFinite(value) && value >= 0`); `export function isTripMember(records: Record<string, unknown>[], callerEmail: string): boolean` (some record's `SK === `USER#${callerEmail}` ``); `export function extractParticipantEmails(records: Record<string, unknown>[]): string[]` (filter `SK` starts with `'USER#'`, map `Email`, filter `Boolean` — verbatim from api.ts:220-223); `export function findItemRecord(records: Record<string, unknown>[], itemId: string): Record<string, unknown> | undefined` (find `SK === `ITEM#${itemId}` ``).

    Add `export interface BuildCreateItemAttributesParams { tripId: string; itemId: string; now: string; body: CreateItemRequest; participantEmails: string[]; }` and `export function buildCreateItemAttributes(params: BuildCreateItemAttributesParams): ItemWriteResult<Record<string, unknown>>`. Order of checks: (1) `body.name?.trim()` non-empty, else error `'name is required'`; (2) `findUnknownFields(body)`, else error `` `Unknown field: ${first}` ``; (3) quantity: `body.quantity === undefined` -> `1`, else `validateNumericField(body.quantity, 'quantity')`; (4) consumable: `body.consumable === undefined` -> `false`, else require `typeof body.consumable === 'boolean'` (error `'consumable must be a boolean'`); (5) weight: `body.weight === undefined` -> omit, else `validateNumericField(body.weight, 'weight')`; (6) status: `body.status === undefined` -> omit, else `isValidItemStatus` (error `'status must be one of to-buy, found, packed'`); (7) packedBy: `body.packedBy === undefined` -> omit, else non-empty trimmed string + `validatePackedByParticipant(trimmed, params.participantEmails)`; (8) `validateStatusRequiresPackedBy(resolvedStatus, resolvedPackedBy)`. On success return `{ ok: true, value: { PK: `TRIP#${params.tripId}`, SK: `ITEM#${params.itemId}`, CreatedAt: params.now, Name: trimmedName, Qty, Consumable, ...(weight !== undefined && { Weight: weight }), ...(packedBy !== undefined && { PackedBy: packedBy.trim().toLowerCase() }), ...(status !== undefined && { Status: status }), ...(body.category !== undefined && { Category: body.category }) } }` — conditional spread only, never a plain object spread of the whole body (D-04/D-05 absent-key discipline).

    In `apps/api/src/routes/api.ts`: extend the `@packpixie/model` barrel import (lines 8-14) to add `Item`, `CreateItemRequest`; add `import { buildCreateItemAttributes, isTripMember, extractParticipantEmails } from '../lib/tripDetail.js';` alongside the existing `mapItemRecord` import (note the required `.js` suffix, CLAUDE.md). Register `protected_.post<{ Params: { tripId: string }; Body: CreateItemRequest }>('/trips/:tripId/items', async (request, reply): Promise<Item> => { ... })` immediately after the existing `GET /trips/:tripId` handler (after line 291, before the closing `});` at line 292). Handler: compute `callerEmail` exactly as the GET handler does; issue the SAME single `QueryCommand` shape (`KeyConditionExpression: 'PK = :pk'`, parameterized `:pk`) to fetch `records`; guard with `isTripMember(records, callerEmail)` -> 404 `{ error: 'Trip not found' }` if false (identical shape to Phase 6, `as never` cast); compute `participantEmails = extractParticipantEmails(records)`; generate `itemId = randomUUID()` and `now = new Date().toISOString()` (already-imported `randomUUID`); call `buildCreateItemAttributes({ tripId, itemId, now, body: request.body, participantEmails })`; on `{ ok: false }` return `reply.status(400).send({ error: result.error }) as never`; on success, issue `new PutCommand({ TableName: conf.dynamoDBTable, Item: result.value })` (add `PutCommand` to the existing `@aws-sdk/lib-dynamodb` import at lines 2-7, alongside `QueryCommand`); return `reply.status(201).send(mapItemRecord(result.value))` — reusing the Phase 6 mapper to build the response guarantees the write and read paths agree on shape (D-04 cross-phase contract, verified by construction).

    In `apps/api/package.json`, change the `"test"` script from `"echo \"Error: no test specified\" && exit 1"` to `"tsx --test src/lib/*.test.ts"` (no new dependency — `tsx` is already a devDependency; Node's built-in `node:test` runner runs through `tsx`'s loader).
  </action>
  <reversibility rating="costly">Implements D-01, the public REST shape (`POST /trips/:tripId/items`) Phase 8's inline-edit table will call against. Changing the route path or response shape later requires a coordinated client update.</reversibility>
  <verify>
    <automated>pnpm --filter api test && pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/tripDetail.test.ts` exists, imports from `./tripDetail.js`, and `pnpm --filter api test` reports all cases passing (TAP `# fail 0`).
    - `packages/model/src/item.ts` exports `CreateItemRequest` with fields `name, quantity?, weight?, packedBy?, status?, category?, consumable?`.
    - `apps/api/src/lib/tripDetail.ts` exports `buildCreateItemAttributes`, and its returned attributes object never contains a `Weight` key when `weight` was omitted from the input, but contains `Weight: 0` when `weight: 0` was explicitly supplied.
    - `apps/api/src/routes/api.ts` registers `'/trips/:tripId/items'` as a POST route inside the `protected_` block, gated by `isTripMember`, using a single parameterized `QueryCommand` with no `IndexName`.
    - `apps/api/package.json` `"test"` script no longer contains the literal `exit 1`.
    - `pnpm --filter api type-check` exits 0.
  </acceptance_criteria>
  <done>A member can POST a valid item body and receive 201 with the created `Item` (Weight/PackedBy/Status/Category present only when supplied); an invalid body (unknown field, packed-without-PackedBy, non-participant PackedBy, non-numeric quantity/weight) is rejected 400 with a specific message; a non-member or unknown tripId receives the identical 404 `{ error: 'Trip not found' }`; `pnpm --filter api test` and `pnpm --filter api type-check` both pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /trips/:tripId/items/:itemId — atomic invariant enforcement</name>
  <files>packages/model/src/item.ts, apps/api/src/lib/tripDetail.ts, apps/api/src/lib/tripDetail.test.ts, apps/api/src/routes/api.ts</files>
  <read_first>
    - apps/api/src/lib/tripDetail.ts (Task 1's output — `ITEM_WRITABLE_FIELDS`, `findUnknownFields`, `isValidItemStatus`, `validateStatusRequiresPackedBy`, `validatePackedByParticipant`, `validateNumericField`, `findItemRecord`, `ItemWriteResult`)
    - apps/api/src/routes/api.ts (the POST handler added in Task 1 — guard pattern, import block)
    - .planning/phases/07-item-write-api-server-authoritative-rules/07-CONTEXT.md (D-05, D-06, D-07 — read the exact rationale for the read-then-write atomic reset before writing `computeItemPatch`)
    - dynamoDB-architecture.md (§5 UpdateItem shape)
  </read_first>
  <behavior>
    Extend `apps/api/src/lib/tripDetail.test.ts` with `computeItemPatch` and `buildUpdateExpression` cases, written before the implementation:
    - Clearing `packedBy` only (`{ packedBy: null }`) against a current record with `Status: 'packed'` yields `removeAttrs` containing both `'PackedBy'` and `'Status'` — the atomic reset (D-05/D-06), with no `status` key in the request body at all.
    - `{ status: 'packed' }` against a current record with no `PackedBy` returns `{ ok: false }` (ITEM-04).
    - `{ status: 'packed' }` against a current record that already has `PackedBy` set (not touched by this patch) returns `{ ok: true }` with `setAttrs.Status === 'packed'`.
    - `{ packedBy: null, status: 'packed' }` in the SAME request returns `{ ok: false }` with the identical `'packed requires PackedBy to be set'` error — no special-case branch, the resulting-field computation naturally produces this (ITEM-05/unclassified).
    - `{ foo: 1 }` returns `{ ok: false, error: 'Unknown field: foo' }`.
    - `{ packedBy: 'x@y.com' }` against a participant list that excludes it returns `{ ok: false }`.
    - `{ weight: null }` yields `removeAttrs` containing `'Weight'`; `{ weight: 0 }` yields `setAttrs.Weight === 0`.
    - `{}` (empty body) returns `{ ok: false, error: 'No fields to update' }`.
    - `buildUpdateExpression({ Name: 'x', Status: 'packed' }, ['Weight'])` returns an `UpdateExpression` containing both a `SET` clause and a `REMOVE` clause, with `ExpressionAttributeNames` mapping an alias (not the literal string `Name` or `Status`) to each real attribute name.
  </behavior>
  <action>
    In `packages/model/src/item.ts`, add `export interface PatchItemRequest { name?: string; quantity?: number; weight?: number | null; packedBy?: string | null; status?: ItemStatus | null; category?: string | null; consumable?: boolean; }` — `null` on the nullable fields means "clear this attribute"; omission means "leave untouched"; `name`/`quantity`/`consumable` are never nullable (they cannot be cleared, only set).

    In `apps/api/src/lib/tripDetail.ts`, add `export interface ItemPatchResult { setAttrs: Record<string, unknown>; removeAttrs: string[]; }` and `export function computeItemPatch(current: Record<string, unknown>, body: Record<string, unknown>, participantEmails: string[]): ItemWriteResult<ItemPatchResult>`. Steps, in order: (1) `findUnknownFields(body)` -> error if any; (2) `Object.keys(body).length === 0` -> error `'No fields to update'`; (3) `packedByCleared = 'packedBy' in body && body.packedBy === null`; (4) resolve `resultingPackedBy`: if `'packedBy' in body`, `null` -> `undefined`, else require non-empty trimmed string + `validatePackedByParticipant`; if absent from body, use `current.PackedBy as string | undefined`; (5) resolve `resultingStatus`: if `'status' in body`, `null` -> `undefined`, else `isValidItemStatus` check; if absent from body, use `packedByCleared ? undefined : (current.Status as ItemStatus | undefined)` — this is the D-06 atomic-reset: clearing PackedBy with no explicit `status` in the same request forces `resultingStatus` to `undefined`; (6) `validateStatusRequiresPackedBy(resultingStatus, resultingPackedBy)` -> error if it fails (this is what naturally rejects `{ packedBy: null, status: 'packed' }` together, with no extra branch); (7) build `setAttrs`/`removeAttrs`: `name` in body -> non-empty trimmed string or error, `setAttrs.Name`; `quantity` in body -> `validateNumericField`, `setAttrs.Qty`; `consumable` in body -> require boolean, `setAttrs.Consumable`; `weight` in body -> `null` -> `removeAttrs.push('Weight')`, else `validateNumericField` -> `setAttrs.Weight`; `packedBy` in body -> `null` -> `removeAttrs.push('PackedBy')`, else `setAttrs.PackedBy = resultingPackedBy`; `('status' in body) || packedByCleared` -> `resultingStatus === undefined` ? `removeAttrs.push('Status')` : `setAttrs.Status = resultingStatus`; `category` in body -> `null` -> `removeAttrs.push('Category')`, else require string -> `setAttrs.Category`. Return `{ ok: true, value: { setAttrs, removeAttrs } }`.

    Add `export function buildUpdateExpression(setAttrs: Record<string, unknown>, removeAttrs: string[]): { UpdateExpression: string; ExpressionAttributeNames: Record<string, string>; ExpressionAttributeValues: Record<string, unknown> }`. Alias EVERY attribute name (not only reserved words — `Name` and `Status` ARE DynamoDB reserved words and would throw `ValidationException` if used unaliased, per 06-PATTERNS.md's Pitfall note; aliasing everything is simplest and safest): for each `setAttrs` key at index `i`, name alias `` `#s${i}` ``, value alias `` `:v${i}` ``, clause `` `#s${i} = :v${i}` ``; for each `removeAttrs` entry at index `i`, name alias `` `#r${i}` ``, clause `` `#r${i}` ``. Join present clause groups as `` `SET ${setClauses.join(', ')}` `` and/or `` `REMOVE ${removeClauses.join(', ')}` ``, separated by a single space when both are non-empty.

    In `apps/api/src/routes/api.ts`: add `UpdateCommand` to the `@aws-sdk/lib-dynamodb` import; add `PatchItemRequest` to the `@packpixie/model` import; import `computeItemPatch`, `buildUpdateExpression`, `findItemRecord` from `'../lib/tripDetail.js'`. Register `protected_.patch<{ Params: { tripId: string; itemId: string }; Body: PatchItemRequest }>('/trips/:tripId/items/:itemId', async (request, reply): Promise<Item> => { ... })` after the POST handler. Handler: same guard query + `isTripMember` 404 as Task 1; `findItemRecord(records, itemId)` -> 404 `{ error: 'Item not found' }` if `undefined`; `computeItemPatch(itemRecord, request.body, extractParticipantEmails(records))` -> 400 on error; `buildUpdateExpression(result.value.setAttrs, result.value.removeAttrs)`; issue `new UpdateCommand({ TableName: conf.dynamoDBTable, Key: { PK: `TRIP#${tripId}`, SK: `ITEM#${itemId}` }, UpdateExpression, ExpressionAttributeNames, ExpressionAttributeValues: Object.keys(ExpressionAttributeValues).length ? ExpressionAttributeValues : undefined, ConditionExpression: 'attribute_exists(PK)', ReturnValues: 'ALL_NEW' })` wrapped in try/catch — on `(error as { name?: string }).name === 'ConditionalCheckFailedException'` return `reply.status(404).send({ error: 'Item not found' }) as never` (guards against the item being deleted between the guard query and this update, preventing UpdateItem's default upsert from creating a phantom row — roadmap SC#4), otherwise rethrow; on success return `reply.status(200).send(mapItemRecord(result.Attributes as Record<string, unknown>))`.
  </action>
  <reversibility rating="costly">Implements the PATCH half of D-01's public route shape — Phase 8's row-save interaction calls this single endpoint for every field combination. Also implements D-06's read-then-write atomic-reset mechanic, which Phase 8 and any future write path must not bypass.</reversibility>
  <verify>
    <automated>pnpm --filter api test && pnpm --filter api type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/tripDetail.ts` exports `computeItemPatch` and `buildUpdateExpression`; `pnpm --filter api test` shows all `tripDetail.test.ts` cases passing, including the `{ packedBy: null, status: 'packed' }` conflict case resolving to a rejection.
    - `apps/api/src/routes/api.ts` registers a PATCH route on `'/trips/:tripId/items/:itemId'` whose `UpdateCommand` includes `ConditionExpression: 'attribute_exists(PK)'` and `ReturnValues: 'ALL_NEW'`.
    - No `ExpressionAttributeNames`-less reference to a literal `Name =` or `Status =` clause exists in the built `UpdateExpression` (both are aliased).
    - `pnpm --filter api type-check` exits 0.
  </acceptance_criteria>
  <done>PATCH accepts any subset of writable fields, enforces packed-requires-PackedBy and the atomic PackedBy-clear/Status-reset in one UpdateItem call, rejects unknown fields and non-participant PackedBy with 400, returns 404 for a non-member or unknown itemId, and returns 200 with the updated `Item`; `pnpm --filter api test` and `pnpm --filter api type-check` both pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DELETE /trips/:tripId/items/:itemId — block-on-packed guard</name>
  <files>apps/api/src/lib/tripDetail.ts, apps/api/src/lib/tripDetail.test.ts, apps/api/src/routes/api.ts</files>
  <read_first>
    - apps/api/src/lib/tripDetail.ts (Task 1/2 output — `findItemRecord`, `isTripMember`)
    - apps/api/src/routes/api.ts (the POST/PATCH handlers added in Tasks 1-2 — guard pattern to replicate)
    - .planning/phases/07-item-write-api-server-authoritative-rules/07-CONTEXT.md (D-08, D-09 — hard delete, block-on-packed)
  </read_first>
  <behavior>
    Extend `apps/api/src/lib/tripDetail.test.ts` with `assertItemDeletable` cases:
    - A record with `Status: 'packed'` returns a non-null error string.
    - A record with `Status: 'to-buy'`, `Status: 'found'`, or no `Status` at all returns `null`.
  </behavior>
  <action>
    In `apps/api/src/lib/tripDetail.ts`, add `export function assertItemDeletable(current: Record<string, unknown>): string | null` returning `'Cannot delete a packed item — unpack it first'` when `current.Status === 'packed'`, else `null`.

    In `apps/api/src/routes/api.ts`, add `DeleteCommand` to the `@aws-sdk/lib-dynamodb` import; import `assertItemDeletable` from `'../lib/tripDetail.js'`. Register `protected_.delete<{ Params: { tripId: string; itemId: string } }>('/trips/:tripId/items/:itemId', async (request, reply): Promise<void> => { ... })` after the PATCH handler. Handler: same guard query + `isTripMember` 404; `findItemRecord(records, itemId)` -> 404 `{ error: 'Item not found' }` if `undefined`; `assertItemDeletable(itemRecord)` -> 400 `{ error: result }` if non-null; issue `new DeleteCommand({ TableName: conf.dynamoDBTable, Key: { PK: `TRIP#${tripId}`, SK: `ITEM#${itemId}` } })`; return `reply.status(204).send()`.
  </action>
  <reversibility rating="one-way">D-08 (already locked in 07-CONTEXT.md): hard delete with no soft-delete/tombstone/undo. A deleted item's data cannot be recovered. Per the decision already made by the developer during discuss-phase, this is recorded here for the audit trail — not re-gated with a checkpoint.</reversibility>
  <verify>
    <automated>pnpm --filter api test && pnpm --filter api type-check && pnpm --filter api build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/tripDetail.ts` exports `assertItemDeletable`; `pnpm --filter api test` shows the new cases passing.
    - `apps/api/src/routes/api.ts` registers a DELETE route on `'/trips/:tripId/items/:itemId'` that returns 400 before issuing any `DeleteCommand` when `assertItemDeletable` returns non-null.
    - `pnpm --filter api build` succeeds with no unused imports left in `api.ts` or `tripDetail.ts` (CLAUDE.md "leave nothing unused behind").
  </acceptance_criteria>
  <done>DELETE on a packed item is rejected 400 with the exact guard message; DELETE on any other existing item removes the row and returns 204; DELETE on an unknown itemId or from a non-member returns the identical 404 shape used by the other two endpoints; `pnpm --filter api test`, `type-check`, and `build` are all green.</done>
</task>

</tasks>

<flagged_assumptions>
Two spec-less edge probes are genuinely not applicable to ITEM-04 and are surfaced here rather than silently dropped:

- **ITEM-04 / adjacency — dismissed:** ITEM-04 is a single-item status-transition invariant (`packed` requires `PackedBy`); there is no interval/sequence/boundary-touching data structure in scope for this phase, so "do they merge, collide, or separate" does not apply.
- **ITEM-04 / ordering — dismissed:** ITEM-04 constrains one boolean relationship between two fields on a single item; there is no multi-element output whose ordering could be specified or asserted.

If the developer disagrees that these are out of scope, raise it before execution.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted bearer token, `tripId`/`itemId` path params, and the full create/patch request body cross here; the token is verified by `authPlugin`, everything else is untrusted input |
| API -> DynamoDB | `tripId`/`itemId` flow into parameterized `Query`/`Update`/`Delete`/`Put` key values; attribute names/values in `UpdateExpression` are built from a fixed, server-controlled key list, never from raw client field names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Elevation of Privilege (IDOR) | POST/PATCH/DELETE item handlers | high | mitigate | Same server-side membership guard as Phase 6: 404 unless a `USER#<callerEmail>` record is present in the query result (`isTripMember`). Access is object-scoped and `tripId`/`itemId` are never trusted as authorization. |
| T-7-02 | Information Disclosure (enumeration) | 404 response paths | high | mitigate | Non-member and unknown-tripId requests return byte-identical `404 { error: 'Trip not found' }`; unknown-itemId on a valid trip returns a distinct but still-404 `{ error: 'Item not found' }` (never 403, never a 200/500 leak). |
| T-7-03 | Tampering (invalid state transition) | `computeItemPatch` / `buildCreateItemAttributes` | high | mitigate | `Status: 'packed'` is rejected whenever the resulting `PackedBy` is unset, computed from the resulting field set (not the raw body) before the write — holds for create, patch, and the packedBy-clear+status-set same-request case (ITEM-04/ITEM-05, D-06). |
| T-7-04 | Spoofing (PackedBy impersonation / mass assignment) | `validatePackedByParticipant`, `findUnknownFields` | high | mitigate | `PackedBy` must resolve to a real `USER#` participant email (D-04); any request-body field outside the fixed writable-field allowlist is rejected 400, so no client can inject `PK`/`SK`/internal keys through the item body (D-05). |
| T-7-05 | Tampering (DynamoDB upsert / phantom row) | PATCH `UpdateCommand` | medium | mitigate | `ConditionExpression: 'attribute_exists(PK)'` on every `UpdateCommand` turns DynamoDB's default upsert-on-missing-key behavior into a `ConditionalCheckFailedException`, mapped to 404 — no phantom row is ever created for a concurrently-deleted item (roadmap SC#4). |
| T-7-06 | Tampering (expression injection) | `buildUpdateExpression` | medium | mitigate | Attribute names/values are never string-concatenated into the expression — every name and value is aliased (`#s0`, `:v0`, ...) via `ExpressionAttributeNames`/`ExpressionAttributeValues`, sourced only from the fixed, server-validated `ITEM_WRITABLE_FIELDS` set. |
| T-7-07 | Repudiation (unrecoverable delete) | DELETE handler | medium | accept | Hard delete with no tombstone is a deliberate, already-locked product decision (D-08); accepted risk is documented, not mitigated with soft-delete machinery this phase. |
| T-7-SC | Tampering (supply chain) | npm installs | low | accept | No new packages installed this phase — the new unit tests run through `tsx`, already an `apps/api` devDependency (RESEARCH Package Legitimacy Audit: not applicable, no install task exists). |

</threat_model>

<verification>
Automated (per task, then per wave): `pnpm --filter api test && pnpm --filter api type-check` after Tasks 1-2; `pnpm --filter api test && pnpm --filter api type-check && pnpm --filter api build` after Task 3; `pnpm lint` across the monorepo at the end of the wave.

Manual guard matrix (M1-M10) — no live HTTP integration harness exists for `apps/api` yet (E2E deferred to Phase 10; a real Cognito JWT and a running DynamoDB Local instance are required for these); execute against `pnpm dev` + DynamoDB Local per README.md before `/gsd-verify-work` (human-verify runs end-of-phase per `human_verify_mode`):

<human-check>
  - **M1 — create happy path:** `POST /trips/:tripId/items` with `{ name: 'Tent', quantity: 1, consumable: false }` as a member -> `201` with an `Item` containing a fresh `itemId`, no `weight`/`packedBy`/`status`/`category` keys.
  - **M2 — create with unknown field:** add `{ foo: 1 }` to the M1 body -> `400 { error: 'Unknown field: foo' }`.
  - **M3 — create packed-without-PackedBy:** `{ name: 'Stove', status: 'packed' }` (no `packedBy`) -> `400 { error: 'packed requires PackedBy to be set' }`.
  - **M4 — create with non-participant PackedBy:** `{ name: 'Rope', packedBy: 'nobody@example.com' }` -> `400 { error: 'PackedBy must be a trip participant' }`.
  - **M5 — patch clear-resets-status:** PATCH an item that is `packed` with `{ packedBy: null }` (no `status` in the body) -> `200`, and the returned `Item` has neither `packedBy` nor `status`.
  - **M6 — patch conflicting clear+packed:** PATCH the same item with `{ packedBy: null, status: 'packed' }` in one request -> `400 { error: 'packed requires PackedBy to be set' }`.
  - **M7 — delete packed item blocked:** mark an item `packed`, then `DELETE` it -> `400 { error: 'Cannot delete a packed item — unpack it first' }`, item still present on a follow-up GET.
  - **M8 — delete unpacked item:** `DELETE` a non-packed item -> `204`, item absent from a follow-up GET.
  - **M9 — 404 parity:** PATCH/DELETE a random `tripId` (as a member of a different trip) and PATCH/DELETE a random `itemId` on a real trip -> both `404`, trip-level 404 body identical to Phase 6's `{ error: 'Trip not found' }`.
  - **M10 — numeric type rejection:** `POST` with `quantity: '3'` (string) -> `400 { error: 'quantity must be a non-negative number' }`; the item is not created.
</human-check>
</verification>

<success_criteria>
- A raw API call cannot set `Status: 'packed'` unless `PackedBy` is set — enforced in both create and patch (roadmap SC#1, ITEM-04).
- Clearing `PackedBy` atomically resets `Status` to unset in one `UpdateItem` call (roadmap SC#2, ITEM-05).
- `Quantity`/`Weight` always persist as DynamoDB numbers; an unset `Weight` is absent, distinct from `0` (roadmap SC#3, ITEM-06).
- Every item mutation requires trip participation and 404s on unknown `itemId`s, with no phantom-row upserts (roadmap SC#4).
- `pnpm test`, `pnpm type-check`, `pnpm lint`, `pnpm build` all green across the monorepo; M1-M10 recorded.
</success_criteria>

<output>
Create `.planning/phases/07-item-write-api-server-authoritative-rules/07-01-SUMMARY.md` when done.
</output>
