---
phase: 06-trip-detail-read-api-participation-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dynamoDB-architecture.md
  - app-architecture.md
autonomous: true
requirements: [TRIP-03]
user_setup: []

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "dynamoDB-architecture.md §2 Participant row shows USER#<email> for the SK, GSI1PK, and GSI1SK identity columns (no USER#<UserId> for membership) (D-07)"
    - "dynamoDB-architecture.md §3 PackedBy description states it stores the participant email; §4.B access pattern reads GSI1PK = USER#<email> (D-07)"
    - "app-architecture.md §5 carries a 'superseded' banner pointing to dynamoDB-architecture.md as authoritative, and §5.2 membership identity references read email (D-08)"
    - "No surviving membership or PackedBy <UserId> identity reference remains in either doc; the corrected docs match the live write path (api.ts:137-141: SK/GSI1PK/Email all lowercased email)"
    - "UsedBy/Carried <UserId> mentions (deferred Distribution features) may remain, each accompanied by a one-line note that participant identity everywhere is the lowercased JWT email (D-07)"
  artifacts:
    - dynamoDB-architecture.md
    - app-architecture.md
  key_links:
    - "docs identity model (email) now matches already-implemented storage (api.ts:137-141) — TRIP-03 is a docs-catch-up, not a data migration"
---

<objective>
Correct the two architecture docs so the documented participant/PackedBy identity matches the already-implemented email-keyed storage (Success Criterion #3, docs half of TRIP-03). `dynamoDB-architecture.md` gets in-scope `UserId`->`email` corrections; `app-architecture.md §5` (a stale multi-table schema that does not match the live single-table design) gets a "superseded" banner plus a §5.2 identity correction.

Purpose: The code and stored data are already email-keyed; only the docs lag. Leaving them stale risks a future implementer reintroducing a non-email identity key and weakening the enumeration-resistant guard.
Output: `dynamoDB-architecture.md` and `app-architecture.md` corrected; no membership/PackedBy `<UserId>` reference survives.
</objective>

<execution_context>
@/Users/mikko.ravimo/git/personal/packPixie/.claude/gsd-core/workflows/execute-plan.md
@/Users/mikko.ravimo/git/personal/packPixie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-trip-detail-read-api-participation-guard/06-CONTEXT.md
@.planning/phases/06-trip-detail-read-api-participation-guard/06-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Documentation only — no code symbols. Corrected files: `dynamoDB-architecture.md` (§2 entity table, §3 PackedBy, §4.B access pattern) and `app-architecture.md` (§5 banner + §5.2 identity).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct dynamoDB-architecture.md in-scope UserId identity references to email (D-07)</name>
  <files>dynamoDB-architecture.md</files>
  <read_first>
    - dynamoDB-architecture.md (the file being modified; §2 entity table line 20, §3 PackedBy line 29, §4.B access pattern line 55; UsedBy/Carried mentions lines 30-31)
    - apps/api/src/routes/api.ts (ground truth for corrected values — the live write path at lines 137-141 writes SK `USER#<creatorEmail>`, GSI1PK `USER#<creatorEmail>`, Email `creatorEmail`, all lowercased via line 115)
    - .planning/phases/06-trip-detail-read-api-participation-guard/06-CONTEXT.md (D-07 exact correction scope)
  </read_first>
  <action>
    In `dynamoDB-architecture.md`, correct only the membership/PackedBy identity references (D-07):
    - §2 entity table (line 20), the Participant row: change the SK `USER#<UserId>`, the GSI1PK `USER#<UserId>` to `USER#<email>` (the GSI1SK is `TRIP#<TripId>` and stays). The corrected values must match the live write path (api.ts:137-141).
    - §3 PackedBy description (line 29): change "Stores the `<UserId>`" to state it stores the participant email (lowercased), keeping the "omit the attribute when unassigned" semantics.
    - §4.B access pattern (line 55): change the Key Condition `GSI1PK = USER#<UserId>` to `GSI1PK = USER#<email>`.
    - Leave the `UsedBy` (line 30) and `Carried` (line 31) `<UserId>` bodies in place (they document deferred Distribution features, out of scope per Phase 5 D-08), but add a single one-line note near §3 stating that participant identity everywhere in the current implementation is the lowercased verified JWT email. Do not otherwise restructure the doc.
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'USER#<UserId>' dynamoDB-architecture.md | grep -viE 'UsedBy|Carried')" && test -z "$(grep -n 'PackedBy' dynamoDB-architecture.md | grep '<UserId>')" && test -n "$(grep -iE 'identity' dynamoDB-architecture.md | grep -iE 'email')"</automated>
  </verify>
  <acceptance_criteria>
    - The §2 Participant row shows `USER#<email>` for SK and GSI1PK; no membership `USER#<UserId>` remains.
    - The §3 PackedBy line states it stores the participant email, and a one-line "identity is the lowercased JWT email" note is present.
    - §4.B reads `GSI1PK = USER#<email>`.
    - No membership/PackedBy `<UserId>` reference survives (grep filtered to exclude the deferred UsedBy/Carried Distribution mentions returns nothing).
  </acceptance_criteria>
  <done>`dynamoDB-architecture.md` documents email-keyed participant identity matching the live write path; the only remaining `<UserId>` strings belong to the deferred UsedBy/Carried Distribution features and are annotated with the identity-is-email note.</done>
</task>

<task type="auto">
  <name>Task 2: Add a superseded banner + §5.2 identity correction to app-architecture.md (D-08)</name>
  <files>app-architecture.md</files>
  <read_first>
    - app-architecture.md (the file being modified; §5 header line 103, §5.1 `ownerUserId` line 113, §5.2 `TripMembers` SK `userId` lines 117-130, and the GSI `userId` line 129)
    - dynamoDB-architecture.md (the authoritative single-table doc the banner must point to)
    - .planning/phases/06-trip-detail-read-api-participation-guard/06-CONTEXT.md (D-08 — superseded-banner approach; a narrower §5.2-only edit is acceptable per Claude's Discretion as long as no stale membership/PackedBy `<UserId>` identity reference survives)
  </read_first>
  <action>
    In `app-architecture.md`, prepend a short "superseded" banner immediately under the §5 header (line 103) stating that §5 describes a stale multi-table schema that does NOT match the implemented design, that `dynamoDB-architecture.md` is the authoritative data-model reference, and that participant identity everywhere is the verified lowercased JWT email (not a `UserId`). Then, in §5.2 `TripMembers` (lines 117-130), correct the identity references so membership keys off email: the SK `userId` (line 120) and the GSI1PK `userId` (line 129) read `email`. Do NOT attempt to reconcile the rest of the fictional multi-table schema (§5.1/§5.3/§5.4 tables, `version` optimistic locking, soft-delete) — the banner marks the whole section superseded, which is sufficient for SC #3. Do not remove any TODO comments.
  </action>
  <verify>
    <automated>grep -niE 'superseded|dynamoDB-architecture' app-architecture.md | head; test -n "$(grep -niE 'superseded' app-architecture.md)"</automated>
  </verify>
  <acceptance_criteria>
    - §5 opens with a "superseded" banner naming `dynamoDB-architecture.md` as authoritative and stating participant identity is the verified lowercased JWT email.
    - §5.2 membership identity references (SK and GSI1PK) read `email`, not `userId`.
    - No membership/PackedBy `<UserId>`/`userId` identity reference in §5 is presented as the current design without the superseding note.
  </acceptance_criteria>
  <done>`app-architecture.md §5` is clearly marked superseded by the single-table doc, its §5.2 membership identity reads email, and a reader is directed to `dynamoDB-architecture.md` for the authoritative model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> future implementer | No runtime boundary; documentation is the only artifact touched. The risk is a future developer trusting stale identity guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-06 | Tampering (of the security contract via stale docs) | dynamoDB-architecture.md / app-architecture.md | low | mitigate | Correcting `UserId`->email removes guidance that could lead a future implementer to reintroduce a non-email membership key, which would weaken the enumeration-resistant guard. The correction itself is the mitigation. |
| T-6-SC | Tampering (supply chain) | npm installs | low | accept | No packages installed — documentation-only plan. |
</threat_model>

<verification>
Automated: the per-task `grep` gates above (no surviving membership/PackedBy `<UserId>` reference; superseded banner present). Wave gate: `pnpm build && pnpm lint && pnpm type-check` (docs changes must not break the monorepo build; they will not, but the gate confirms no accidental code edit).

Doc review (end-of-phase human-verify):
<human-check>
  - Read the corrected §2/§3/§4.B of `dynamoDB-architecture.md` and confirm the documented identity matches the live write path (`api.ts:137-141`).
  - Read the §5 banner in `app-architecture.md` and confirm a reader is unambiguously directed to `dynamoDB-architecture.md` as authoritative.
</human-check>
</verification>

<success_criteria>
- No membership/PackedBy `<UserId>` identity reference survives in either doc (SC #3).
- `dynamoDB-architecture.md` and `app-architecture.md §5/§5.2` document the email-keyed identity that the code already implements.
- `pnpm build` remains green (no accidental code change).
</success_criteria>

<output>
Create `.planning/phases/06-trip-detail-read-api-participation-guard/06-02-SUMMARY.md` when done.
</output>
